# CPU-only build (no NVIDIA GPU required)
# Install with:  pip install -r requirements_cpu.txt
# The --index-url line pulls the CPU build of torch (no CUDA, much smaller).

--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core models (CPU build) ---
torch==2.2.0
torchvision==0.17.0
facenet-pytorch==2.5.3   # MTCNN + InceptionResnetV1

# --- Computer vision ---
opencv-python-headless==4.9.0.80
Pillow==10.2.0

# --- Data & utils ---
numpy==1.26.4

# --- GUI / output / build ---
PyQt5==5.15.10           # desktop GUI
openpyxl==3.1.2          # export attendance logs to Excel
pyinstaller==6.5.0       # build exe

# --- Text-to-speech (voice announce, Windows SAPI5) ---
pyttsx3==2.90
pywin32==306