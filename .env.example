# 이 파일을 .env 로 복사한 뒤 환경에 맞게 수정하세요.
#   cp .env.example .env
#
# SQLite DB 경로를 코드에 하드코딩하지 않고 여기서 주입합니다.
# python-dotenv 가 main.py / registry.py / database.py 에서 자동 로드합니다.

NAS_DB_PATH=/mnt/nas/attendance.db
